# Nightly backlog

Shared backlog for the autonomous scheduled jobs that operate on this repo
(`daily-repo-improvement`, `notes-to-factory`). Read this **before** proposing
work. Items under "Open" are already proposed; items under "Checked, not
applicable" were already ruled out. Re-proposing either is a failure.

## Open

- [ ] 2026-08-22 — **Factory Learning V1: signals → clustering → improvement candidates → human review → governed experiment → promotion recommendation** — Jay's stated next value jump; the learning loop is what turns "many strong components" into a governed factory. Must not self-authorize, mutate governance, bypass verification, or become a token sink. Source: apple-note p7000.
- [ ] 2026-08-22 — **Mission Control System Qualification Run (E2E)** — one realistic Mission traversing Mission → Plan → recipe recommendation → FactoryVersion → Context Package → worker lease → execution → trace → source Attempt → candidate PR → independent verification Attempt → frozen Verification Plan → evidence/receipts → human acceptance → Learning signal → improvement candidate → experiment proposal. Must also exercise negative paths: stale worker lease, candidate/PR-head mismatch, verification failure, retry lineage, context miss, deterministic gate failure, sandbox orphan, model-routing override. Goal is proving architectural invariants survive subsystem boundaries, not that "the app works." Jay suggests a dedicated stream carrying no new product features. Source: apple-note p7000. **Measured 2026-08-31:** `scripts/system-factory-e2e-qualification.mjs` (236 lines) mentions `lease` and `deterministic` but contains no reference to stale leases, candidate/PR-head mismatch, verification failure, retry lineage, context miss, sandbox orphan, or model-routing override — so none of the listed negative paths are covered yet. The happy path is the only thing this item can currently claim.
- [ ] 2026-08-22 — **Factory Memory & Context Intelligence (5 phases)** — Hybrid Factory RAG, Typed Factory Relationships, Agentic Retrieval, Factory Knowledge Graph, Autonomous Context Engineering. A core subsystem, not a sidecar chatbot or generic enterprise search. Prefer additive schema changes and existing domain concepts; do not introduce an external vector/graph/observability platform as a hard dependency. Source: apple-note p6971.
- [ ] 2026-08-22 → narrowed 2026-09-02 — **Full repository audit + E2E qualification pass** — reconnaissance-first review of intent vs. implementation across architecture, contracts, migrations, workers, executors, adapters, verification, sandbox, observability, authz, and feature flags, before any editing. Large; run as its own governed stream. Source: apple-note p7018. **Remaining scope excludes** the credential-detection surface audited on 2026-08-31, the orchestration-server ingress surface, and the Convex tool-risk gate audited on 2026-09-02 (see Closed). Authorization remains in scope but should start from the known baseline of 637 unauthorized public Convex functions reported by `pnpm run security:authorization`, not from a fresh enumeration.
- [ ] 2026-08-23 — **IP → Software Factory: "Book Factory" vertical on Mission Control** — Jay's thesis is that intellectual property is becoming executable, and books are the first wedge. A book enters as a WorkOrder and traverses a Rights + Licensing gate → book understanding → product opportunity analysis → generated products (book site, grounded chat, methodology copilot, courses, assessments, chapter media via an LTX-style video capability registered as a callable Factory capability). Explicitly *not* a manual per-book team: the point is that Mission Control is the machinery that makes launching hundreds of these economically viable, and that a cross-book flywheel makes book #51 better than book #1. Jay's stated de-risking step is building three public-domain works (The Art of War, Meditations, The Prince) end-to-end through the Factory before approaching any author. Large: new vertical, new gate type (rights/licensing), new capability class (media generation), new cross-mission learning surface. Do not implement incrementally as a side feature — it needs its own governed stream and a rights model decided first. Source: apple-note p7068.

- [ ] 2026-09-02 — **Provider SDK pins: one unused, one far behind** — `@anthropic-ai/sdk@0.26.1` is declared in `packages/model-router/package.json` (`^0.26.0`) but **nothing imports it**: `git grep "@anthropic-ai/sdk" -- '*.ts' '*.mjs' '*.js'` returns nothing; `providers/claude.ts` calls `fetch(".../v1/messages")` directly. Remove the dependency rather than upgrade it (lockfile change, so paired with the next `pnpm install` touch). `openai@4.104.0` (`^4.73.0` in `apps/orchestration-server/package.json`) has exactly one call site, the dynamic `import("openai")` in the `/classify` route of `index.ts`, plus an `any` shim in `shims.d.ts`. No advisory on either (the 2026-09-01 sweep found zero npm advisories for `openai`). The 4.x → current major bump is small in surface but changes the `ws` peer, so check it against the root `ws@8` override; also a candidate for replacing with the same raw-`fetch` pattern the model-router uses, which would drop the dependency entirely.

- [ ] 2026-08-31 — **Telegram bot has no operator authentication** — `packages/telegram-bot` contains no allowlist, admin check, or chat-id gate anywhere in `src/`. Any Telegram user who can reach the bot may run `/switch <slug>` and then `/approve <id-suffix>`; `decidedByUserId` is recorded as the caller's unverified Telegram username. The 2026-08-31 fix scoped decisions to the selected workspace but deliberately did not add authentication, because the right shape (operator allowlist vs. linking Telegram identities to Convex users) is a product decision. Decide the shape before implementing.
- [ ] 2026-08-31 — **`approvals.deny` accepts no `projectId`, so it cannot verify workspace scope** — `convex/approvals.ts` gives `approve` an optional `projectId` and checks it at line 400 (`if (args.projectId && ...)`), but `deny` has no such argument and performs no scope check at all. Both checks are also opt-in: omitting `projectId` skips them. Making the argument required is a public-interface change across the UI, orchestration server, and bot callers, so it needs a coordinated change rather than a nightly patch.
- [ ] 2026-08-31 — **`packages/telegram-bot` has no test harness** — no `test` script and no vitest devDependency, so all 8 source files (1420 lines, including the approval command handlers) are unreachable by `pnpm run test`. Wiring vitest in would let the approval path carry regression tests; it was left alone tonight because it changes `pnpm-lock.yaml` and the root recursive test run.


## Checked, not applicable

- 2026-09-02 — **`@modelcontextprotocol/sdk` CVE-2026-0621 (ReDoS, fixed 1.25.2) and CVE-2026-25536 (cross-client data leak via a shared `StreamableHTTPServerTransport` / `McpServer`, fixed 1.26.0)** — the lockfile resolves exactly `1.26.0`, i.e. the fix version, but the version check is not the point. The package is not a direct dependency of anything in the workspace: it arrives only via root devDependency `@taskmasterai/cli@1.1.4` → `fastmcp@3.33.0`, and `git grep` finds no first-party import of `@modelcontextprotocol/sdk`, `fastmcp`, `McpServer` or `StreamableHTTPServerTransport` under `apps/`, `packages/`, `convex/` or `scripts/`. There is no MCP server or transport instantiated by this codebase, so there is nothing that could be shared across clients; the only code that would run it is the Taskmaster CLI on a developer's machine.

- 2026-08-22 — **Basic / Intermediate / Advanced experience toggle** — requested twice in Jay's notes (p6972: "a toggle for basic, intermediate and advanced so folks don't get overwhelmed"; p7000: "Basic gets a few high-confidence recommendations; Intermediate gets evidence/impact; Advanced gets raw traces/evals/config drift/experiment history"). **Already implemented**: `apps/mission-control-ui/src/factoryExperience/` provides `ExperienceLevelSelector`, `useFactoryExperienceLevel`, and `ProgressiveFactoryView`, with `FactoryExperienceLevel` gating in `CreateFactoryMissionDialog`, plus coverage in `tests/e2e/review-intelligence.e2e.spec.ts` and `tests/e2e/system-factory-qualification.e2e.spec.ts`. No work required.

## Closed
- [x] 2026-09-02 → 2026-09-02 — `/gateway/ws` bypassed orchestration-server authentication — Fixed on `nightly/2026-09-02-improvements` (PR #156): the proxy takes an `authorizeUpgrade` hook wired to `orchestrationUpgradeFailure`, the same bearer rule as `requireAuth()` (401 wrong/missing bearer, 503 in production without a token, open only in tokenless dev); refused upgrades get a real HTTP response. The Vite dev proxy now presents the bearer on `/gateway`; a production reverse proxy must do the same (documented in TROUBLESHOOTING). Chosen shape (2)-adjacent: header-based, no ticket endpoint, because the browser already never held the token — a proxy did.

<!-- - [x] YYYY-MM-DD → YYYY-MM-DD — <initiative> — <how it was resolved> -->

- [x] 2026-08-22 → 2026-09-02 — **Ingress + tool-risk-gate audit (authz slice of the full repository audit)** — audited two guards against their whole input space rather than confirming they exist. (1) `convex/lib/riskClassifier.ts` claimed to be "synced with packages/policy-engine/src/rules.ts" but used a different tool vocabulary; the persona files under `agents/` and `roles/` declare the policy-engine names, so ten tools the policy engine rates RED (`deploy_prod`, `drop_table`, `destructive_shell`, `read_env`, `access_secrets`, `post_social`, `tweet`, `post_linkedin`, `modify_prod_config`, `external_api_write`) fell through to the YELLOW default, which `requiresApproval` auto-allows for SPECIALIST and LEAD. Reproduced (`classifyRisk("deploy_prod") === "YELLOW"`), fixed by adding the RED entries (escalation only), and guarded by a parity test that imports the policy-engine map so the two cannot drift downward again; the false header comment was corrected. (2) `apps/orchestration-server` ingress: `auth.ts` is sound (constant-time compare, fail-closed 503 in production without a token, explicit public-route allowlist), but the `/gateway/ws` upgrade path never passes through it — recorded as an Open design item above rather than patched, because the browser has no credential to present. Along the way the proxy's upstream-close handler was found to read a browser `CloseEvent` shape off the Node `ws` positional `(code, reason)` arguments, so every rejected connect surfaced as `Upstream gateway closed (undefined):`; fixed with a test. Characterization tests were added for `riskClassifier`, `gateway-proxy`, `getActiveTenant` (tenant precedence and the arbitrary-active-tenant fallback for legacy rows) and `sanitize`. Also checked: `packages/research-adapters/src/networkPolicy.ts` (SSRF guard) is already covered by `webRssAdapter.test.ts` and handles IPv4-mapped IPv6, CGNAT, link-local, documentation and multicast ranges with IP pinning across redirects; the only ranges it does not classify as private are NAT64 `64:ff9b::/96` and 6to4 `2002::/16`, which a public DNS answer would not normally carry.
- [x] 2026-08-22 → 2026-08-31 — **Credential-detection audit (reconnaissance slice of the full repository audit)** — audited both credential guards against their whole input space rather than confirming that a guard exists. `scripts/lib/repository-secret-scan.mjs` and `packages/shared/src/factoryMemorySecurity.ts` independently matched classic GitHub tokens with `gh[pousr]_` character classes that structurally cannot match fine-grained PATs (`github_pat_<base62>_<base62>`), and neither detected Telegram bot tokens despite `packages/telegram-bot` shipping in this repo. Both gaps were reproduced with synthetic tokens, fixed, and given unit tests; the repository scan still passes clean over all 2647 tracked files. Also ruled out: the UI is Vite + React with no Next.js dependency, so the August 2026 Next.js AVIF RCE does not apply here, and the only non-`VITE_` secret read in `apps/mission-control-ui/vite.config.ts` (`ORCHESTRATION_API_TOKEN`) is consumed in `server.proxy`, which runs in Node during dev and is never inlined into the browser bundle.

---

## Governance note for autonomous jobs

This repository's own architectural invariants, as recorded in Jay's design
notes (apple-note p6971), include:

> Humans merge PRs. No full-auto.
> `workOrders.accept` remains the only WorkOrder acceptance authority.
> Executors do not self-certify acceptance.
> Agent completion does not equal verified success.

Automated jobs operating on this repo should therefore open pull requests
rather than merging to `main` directly, even when all checks are green.
